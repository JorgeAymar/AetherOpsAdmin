#!/usr/bin/expect
set timeout 60
set host "194.163.161.196"
set user "sysadmin"
set password "SysAdmin2025!"

spawn ssh -o StrictHostKeyChecking=no $user@$host "echo 'Hello from VPS'; uptime"
expect {
    "password:" { send "$password\r" }
    timeout { puts "SSH Connection timed out"; exit 1 }
}
expect eof
